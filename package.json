{
  "name": "guv-nexus-gh-pages-snapshot",
  "private": true,
  "version": "1.0.0",
  "description": "Pre-built static snapshot served by GitHub Pages. The build step only assembles the dist folder Cloudflare Pages expects.",
  "scripts": {
    "build": "mkdir -p dist && cp index.html 404.html dist/ && cp -r assets dist/"
  }
}
